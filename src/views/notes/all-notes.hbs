<div class="row">
    {{#each notes}}
        <div class="col-md-3">
            <div class="card bg-warning">
                <div class="card-body">
                    <h4 class="text-danger card-title d-flex justify-content-between align-items-center">{{this.title}} <a href="/notes/edit/{{_id}}"><i class="fa fa-edit"></i></a></h4>
                    <p class="text-primary">{{this.description}}</p>
                </div>
                <div class="card-footer">
                    <form action="/notes/delete/{{_id}}" method="POST">
                    <button class="btn btn-danger btn-block btn-sm">DELETE</button>
                    </form>
                </div>
            </div>
        </div>
    {{/each}}
</div>